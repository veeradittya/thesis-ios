import { MonacoHome } from "@/components/MonacoHome";

// Keep the full pre-landing-page product available while the public root is in coming-soon mode.
export default function DashboardPage() {
  return <MonacoHome />;
}
