Run the daily briefing. Read your memory FIRST. Research each stock in the `holdings` table AT MOST ONCE PER DAY: skip any ticker whose assets.researched_at is within the last ~20 hours; otherwise research it (Oddpool order books/whale/odds, Tier-A news, SEC filings, anomaly code, cross-signal correlation) and UPSERT its SHARED row in `assets` (verdict; risk 0-100 where higher = more risk; a plain-language rationale of what happened and how it affects the owner, with linked sources; signals JSON for the numbers; and a VERY SUCCINCT plain-language analyst_brief on where Wall Street analysts stand, with NO em dashes and NO links). After every stock is fresh, do the PORTFOLIO pass per user: run THESIS WATCH for each holding that has a `holdings.thesis` (decompose it into assumptions + leading indicators, check them against today's evidence and a targeted search, grade intact/watch/stressed, and upsert the `theses` row), then write the portfolio's KEY TAKEAWAYS into `portfolios.memo` (each user_id in holdings) — 1-3 SHORT points, ONE PER LINE (newline-separated), most important first, each a tight key sentence (<=~12 words, ends with a period) plus AT MOST one short context clause. LEAD with a thesis line ONLY when a thesis is under stress (watch/stressed) and not already alerted; stay SILENT on any thesis that is intact — never say a thesis "holds". No run number or timestamp prefix. Reply with the takeaways.

As the FINAL step, ONLY after every portfolio memo has been written, fire the brief-ready push notifications with exactly ONE request: `curl -sS -X POST https://betathesis.com/api/push/send-brief -H "x-push-secret: $PUSH_SEND_SECRET"`. PUSH_SEND_SECRET is injected from your vault as an environment variable (never printed). Do this once, at the very end; a non-2xx response is non-fatal, so do NOT retry it in a loop. This is what tells each user their brief is ready, so it must run after the memos, not before.
